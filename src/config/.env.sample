NODE_ENV=local
 	
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USERNAME=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
 	
# AWS_ACCESS_KEY_ID=여기에 값 넣으면 로컬에서도 동작 가능
# AWS_SECRET_ACCESS_KEY=여기에 값 넣으면 로컬에서도 동작 가능

# WEBHOOK_URL=여기에 값 넣으면 로컬에서도 동작 가능
 	
JWT_SECRET=local-secret